<resources>

    <!-- Base application theme. -->
    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/davysGrey</item>
        <item name="colorPrimaryDark">@color/graniteGray</item>
        <item name="colorAccent">@color/platinum</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="AppTheme.SplashScreen" parent="AppTheme.NoActionBar">
        <item name="android:windowDisablePreview">true</item>
    </style>

    <style name="Divider">
        <item name="android:layout_height">match_parent</item>
        <item name="android:layout_width">5dp</item>
        <item name="android:background">?android:attr/listDivider</item>
        <item name="android:color">@color/platinum</item>
    </style>
</resources>
